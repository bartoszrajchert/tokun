import { describe, it } from "vitest";

// TODO: finish tests
describe("Test DTCG JSON Loader", () => {
  it.todo("should load and parse JSON content");
  it.todo(
    "should throw error for invalid JSON content and provide error message",
  );
});
