---
title: "Basic"
order: 1
---

# Examples — Basic

The basic template is intentionally small and shows strict DTCG 2025.10 usage:

- structured color values (`colorSpace` + `components`)
- group root tokens with `$root`
- curly-brace aliases
- gradient positions in the `[0, 1]` range

Source: `examples/templates/basic/basic.tokens.json`

```json
{
  "brand": {
    "color": {
      "$type": "color",
      "$root": {
        "$value": {
          "colorSpace": "srgb",
          "components": [0.0706, 0.2039, 0.3373],
          "hex": "#123456"
        }
      }
    }
  }
}
```

For more complete setups, see the advanced template.
